const checkNumber = () => {
  // =========== ボールの数字とカード上の数字のチェックをして =============
  // ======== 一致した場合、カード上のその数字の背景色を変更する関数 =========
  // 引いた数字の値がカード上にある場合、
  // if (cardNumArray.includes(this.ballNumArray[0])) {
  //   // インデックス番号を取得する
  //   const indexNum = cardNumArray.indexOf(this.ballNumArray[0]);
  //   // 背景色を変える＝穴を開ける
  //   col_BINGO[indexNum].ariaChecked = "true";
  // }
  // showReachBing();
};

export default checkNumber;
